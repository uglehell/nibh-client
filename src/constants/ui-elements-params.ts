export enum EButtonColors {
  default = 'default',
  red = 'red',
  blue = 'blue',
}

export enum EButtonTypes {
  button = 'button',
  reset = 'reset',
  submit = 'submit',
}

export enum EInputTypes {
  text = 'text',
  password = 'password',
}
